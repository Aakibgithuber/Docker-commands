# A minimal "Hello World" image used by the Jenkins pipeline demo.
# Alpine is a tiny base image, so this builds and pushes quickly.
FROM alpine:3.20

# Print a friendly message when the container runs.
CMD ["echo", "Hello, World! This image was built and pushed by a Jenkins pipeline."]
